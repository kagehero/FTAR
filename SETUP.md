# Supabase設定ガイド

## 環境変数の設定手順

### 1. Supabaseプロジェクトの作成

1. [Supabase](https://supabase.com)にアクセスしてアカウントを作成（またはログイン）
2. 新しいプロジェクトを作成
3. プロジェクトの作成が完了するまで待つ（数分かかる場合があります）

### 2. 環境変数の取得

1. Supabaseダッシュボードでプロジェクトを開く
2. 左側のメニューから **Settings** → **API** を選択
3. 以下の情報をコピー：
   - **Project URL** → `NEXT_PUBLIC_SUPABASE_URL` に使用
   - **anon public** キー → `NEXT_PUBLIC_SUPABASE_ANON_KEY` に使用

### 3. .env.localファイルの作成

プロジェクトルート（`package.json`がある場所）に`.env.local`ファイルを作成し、以下の内容を記入：

```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxx
```

**重要**: 
- `https://`で始まる完全なURLを設定してください
- キーは`eyJ`で始まる長い文字列です
- 値の前後に余分なスペースや引用符を付けないでください

### 4. 開発サーバーの再起動

環境変数を変更した後は、**必ず開発サーバーを再起動**してください：

```bash
# 現在のサーバーを停止（Ctrl+C）
# その後、再起動
npm run dev
```

### 5. 設定の確認

ブラウザのコンソール（F12 → Console）を開いて、以下のメッセージが表示されることを確認：

```
✅ Supabase設定が確認されました
```

もし以下のメッセージが表示される場合は、設定を見直してください：

```
⚠️ Supabase環境変数が設定されていません。
```

## トラブルシューティング

### エラー: "サーバーに接続できませんでした"

1. **.env.localファイルが正しい場所にあるか確認**
   - プロジェクトルート（`package.json`と同じ階層）に配置されているか

2. **環境変数の値が正しいか確認**
   - URLは`https://`で始まっているか
   - キーが完全にコピーされているか（途中で切れていないか）
   - 余分なスペースや改行が入っていないか

3. **開発サーバーを再起動したか確認**
   - 環境変数を変更した後は必ず再起動が必要です

4. **Supabaseプロジェクトがアクティブか確認**
   - Supabaseダッシュボードでプロジェクトの状態を確認
   - プロジェクトが一時停止していないか

5. **ネットワーク接続を確認**
   - インターネット接続が正常か
   - ファイアウォールやプロキシがSupabaseへの接続をブロックしていないか

### エラー: "Supabaseの設定が完了していません"

`.env.local`ファイルが存在しないか、環境変数が正しく設定されていません。

1. `.env.local`ファイルがプロジェクトルートに存在するか確認
2. ファイルの内容が正しいか確認
3. 開発サーバーを再起動

## データベースのセットアップ

環境変数の設定が完了したら、`supabase-setup.sql`ファイルの内容をSupabaseのSQLエディタで実行してください。

1. Supabaseダッシュボードで **SQL Editor** を開く
2. `supabase-setup.sql`の内容をコピー＆ペースト
3. **Run** ボタンをクリックして実行

これで`profiles`テーブルと必要なポリシーが作成されます。
