## FTAR 利用マニュアル

この文書は、FTAR（振替・出欠管理システム）の**一般ユーザー（会員）**と**管理者**向けの簡易マニュアルです。

---

## 1. ロールとログイン

- **ロール**
  - **会員（member）**: 自分の出欠・振替・マイページを操作します。
  - **管理者（admin）**: クラス・開催日・会員・振替・通知など全体を管理します。

- **ログイン/ログアウト**
  - アプリのトップ `http://localhost:3000/` を開きます。
  - 登録済みのメールアドレスとパスワードでログインします。
  - ログイン後の遷移先:
    - 会員: `/member/home`
    - 管理者: `/admin/dashboard`
  - 画面右上の「ログアウト」ボタンからログアウトできます。

- **新規登録（会員のみ）**
  - 画面下部の「新規登録」リンクから `/register` へ移動します。
  - メールアドレス・パスワードを入力して登録すると、自動的に**会員**として作成されます。

---

## 2. 会員向け機能

### 2-1. 会員ホーム `/member/home`

- 本日の自分のクラスと出欠状況を確認できます。
- 表示内容:
  - 本日のクラス名・時間・会場
  - 出欠ステータス（未登録 / 出席 / 欠席）
  - 振替チケット残数
  - お知らせ一覧
- 今日のクラスの出欠ボタンから、**出席 / 欠席**を登録できます（開始1時間前まで変更可）。

### 2-2. 出欠登録

- 会員ホームまたは対象クラスから出欠を変更します。
- ルール:
  - クラス開始 **1時間前まで** 出席⇔欠席の変更が可能です。
  - 欠席にすると、自動的に**振替チケット**が発行されます。

### 2-3. 振替チケット一覧 `/member/transfer-tickets`

- 自分の振替チケットが一覧表示されます。
  - 未使用 / 使用済み / 失効 の3カテゴリに分かれます。
- 「振替先を選択」ボタンから、チケットを利用して振替予約ができます。

### 2-4. 振替先選択 `/member/transfer/select/[ticketId]`

- 振替に利用可能なクラス一覧が表示されます。
- ルール:
  - 対象学年・カテゴリが合うクラスのみが候補になります。
  - 定員に空きがあるクラス: すぐに**振替確定（出席）**になります。
  - 満席のクラス: **キャンセル待ち**として登録されます。

### 2-5. キャンセル待ち一覧 `/member/waitlist`

- 自分がキャンセル待ち登録しているクラスが表示されます。
- 各項目で以下を確認できます:
  - クラス名・日付・時間
  - 現在の順番
  - ステータス（waiting / confirmed / expired）

### 2-6. マイページ `/member/mypage`

- 自分のプロフィールと履歴を確認できます。
  - プロフィール: 氏名・学年・メールアドレス・電話番号（必要に応じて編集）
  - 出席履歴: 過去の出欠状況
  - 振替履歴: 振替チケットの発行・使用履歴

---

## 3. 管理者向け機能

### 3-1. 管理ダッシュボード `/admin/dashboard`

- ログイン後のトップ画面です。
- 表示内容:
  - 管理機能メニュー（クラス管理 / 開催日管理 / 会員管理 / 振替管理 / メール配信）
  - 本日のクラス一覧
    - クラス名・日付・時間・会場
    - 出席予定人数・欠席人数
    - 「出欠名簿を表示」ボタン（出欠名簿画面へ）

### 3-2. クラス管理 `/admin/classes`

- クラスの追加・編集・無効化を行います。
- 入力項目:
  - クラス名
  - 曜日
  - 開始時刻・終了時刻
  - 対象学年
  - カテゴリ（例: 初心者 / 上級）
  - 定員
  - 会場
  - 振替を許可するか
- 無効化したクラスは、以降の自動生成や振替先候補から除外されます。

### 3-3. 開催日管理 `/admin/class-dates`

- クラスごとの**開催日**を管理します。
- 機能:
  - **自動生成**:
    - クラスと期間（開始日～終了日）を指定すると、曜日に合わせて開催日が一括生成されます。
  - **手動作成**:
    - 単発の開催日を手動で追加します。
  - **中止 / 再開**:
    - 開催日を中止にすると、該当日の出席登録が欠席扱いになり、設定に応じて振替チケットが自動発行されます。
    - 再開にすると、中止フラグが解除されます。
  - 「名簿」ボタンから、該当日の出欠名簿画面へ移動できます。

### 3-4. 出欠名簿 `/admin/class/[classDateId]/attendance`

- 指定した開催日の出欠状況を一覧で管理します。
- 表示:
  - 出席者一覧
  - 欠席者一覧
  - 未登録者一覧（学年など条件に合う会員）
  - キャンセル待ち一覧
- 操作:
  - 各会員の出席⇔欠席を**管理者が手動で変更**できます。
  - 未登録者を出席または欠席として登録できます。
- **印刷用名簿**:
  - 画面右上の「印刷」ボタン、またはブラウザの印刷機能から印刷します。
  - A4縦の名簿レイアウトで、クラス名・日付・時間と、No/氏名/学年/出席確認欄/備考欄が出力されます。

### 3-5. 振替管理 `/admin/transfers`

- 振替チケットの利用状況を一覧で確認します。
- 機能:
  - ステータス（すべて / 未使用 / 使用済み / 失効）でフィルタリング
  - 各チケットについて:
    - 会員名
    - 欠席したクラス・日付
    - 振替先クラス・使用日（使用済みの場合）
    - 有効期限・現在の状態

### 3-6. 会員管理 `/admin/members`

- 会員の一覧と詳細を管理します。
- 一覧機能:
  - 名前・メールアドレスで検索
  - 学年フィルタ
  - 状態フィルタ（在籍 / 休会 / 退会）
- 詳細画面 `/admin/members/[id]`:
  - 基本情報（氏名・メール・学年・電話番号・状態）
  - 出席履歴
  - 振替履歴
  - 通知履歴（メール / LINE）
  - 編集:
    - 管理者が、氏名・学年・電話番号・状態・アクティブフラグを変更できます。

### 3-7. メール配信 `/admin/notifications`

- 会員への一斉連絡・クラス別連絡に使います（本プロジェクトでは送信ログのみ実装、実際のメール送信は別途連携が必要です）。
- 通知送信:
  - 送信先:
    - 全会員
    - 特定クラスの出席者
    - 特定クラスの欠席者
  - 通知タイプ:
    - メール / LINE（ログ上の区別のみ）
  - 件名・本文を入力して送信します。
- 送信履歴:
  - 送信日時
  - 種類（メール / LINE）
  - 件名または本文の冒頭
  - ステータス（送信済み / 送信失敗）

---

## 4. よくある質問（FAQ）

- **Q1. 会員が管理画面にアクセスするとどうなりますか？**
  - A. ロールが `member` の場合、`/admin/*` へアクセスすると会員ホームかログイン画面へリダイレクトされます。

- **Q2. ログインしていない状態で会員・管理画面にアクセスすると？**
  - A. 未認証の場合はトップ（ログイン）画面にリダイレクトされます。

- **Q3. 振替チケットの有効期限はどこで確認できますか？**
  - A. 会員は `/member/transfer-tickets` で、管理者は `/admin/transfers` で確認できます。

- **Q4. 実際のメール送信は行われますか？**
  - A. 現時点の実装では、メール送信は**ログ記録のみ**です。外部のメールサービスと連携する場合は、`/api/admin/notifications/send` に実際の送信処理を追加してください。

---

## 5. 動作確認のポイント

- ブラウザで以下を確認してください:
  - 会員:
    - ログイン → `/member/home` で本日の予定と出欠が見えるか
    - 出欠登録 → 振替チケット発行・利用が正しく動くか
    - マイページで履歴が正しく見えるか
  - 管理者:
    - `/admin/dashboard` からすべての管理機能へ遷移できるか
    - クラス / 開催日 / 会員 / 振替 / 通知 / 名簿印刷が一通り操作できるか

不明点や追加したい機能があれば、このマニュアルに追記して運用してください。

